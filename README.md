`train_phone_finder.py`   takes   a   single   command   line   argument   which   is   a   path   to   a
folder   with   labeled   images   and a file called   `labels.txt`.   
```
python train_phone_finder.py ./data 
```
